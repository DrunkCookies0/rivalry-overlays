node_modules/
dist/
dist-beta/
.verify/
*.log
.DS_Store
.recon/
LeagueOS/
RIVALRY-web/
rockpload/
Recordings/
# Raw designer drops (Moldybanana's Summer 2026 art). Only the ported, signed
# scenes + optimized art get committed — the raw folder is 35MB of dupes.
Moldy_Overlays/
*.exe
*.mp4

# Overlay signing — public key is committed, private key never is
overlays/keys/*-private.pem

# Retired RCV1 access-key material (system removed 2026-08-11). The folder may
# still hold the old private key and issuance records on dev machines; it never
# leaves this machine and nothing reads it any more.
keys/
