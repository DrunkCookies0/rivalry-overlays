node_modules/
dist/
dist-beta/
.verify/
*.log
.DS_Store
.recon/
LeagueOS/
RIVALRY-web/
rockpload/
Recordings/
# Raw designer drops (Moldybanana's Summer 2026 art). Only the ported, signed
# scenes + optimized art get committed — the raw folder is 35MB of dupes.
Moldy_Overlays/
*.exe
*.mp4

# Overlay signing — public key is committed, private key never is
overlays/keys/*-private.pem

# Casterverse access keys — the public key ships (config/), the private key and
# the record of who holds a key never leave this machine
keys/
